# 轻量化部署依赖包 - 适用于蜜罐主机
# 总安装大小: ~150MB (相比标准版本减少60%)

# FastAPI 核心框架 (必需)
fastapi==0.104.1
gunicorn==21.2.0  # 替代uvicorn，更稳定
uvicorn[standard]==0.24.0  # 作为worker使用

# 轻量化数据库 (SQLite)
aiosqlite==0.19.0  # 异步SQLite支持
sqlalchemy==2.0.23
alembic==1.12.1

# 认证和安全 (必需)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# 配置管理 (必需)
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP 客户端 (轻量化)
httpx==0.25.2
aiofiles==23.2.1

# 日志 (简化)
python-json-logger==2.0.7

# 系统监控 (轻量化)
psutil==5.9.6  # 系统资源监控

# 开发工具 (可选)
# pytest==7.4.3
# black==23.11.0
# isort==5.12.0

# 其他必需工具
python-dateutil==2.8.2
email-validator==2.1.0
setproctitle==1.3.3  # 进程标题设置

# 移除的重型依赖 (相比标准版本):
# - psycopg2-binary (PostgreSQL驱动)
# - asyncpg (异步PostgreSQL)
# - celery (任务队列)
# - redis (缓存)
# - pymysql (MySQL驱动)

# 可选的性能优化包
# orjson==3.9.10  # 更快的JSON序列化 (可选)
# ujson==5.8.0    # 更快的JSON解析 (可选)
